{
  "name": "fake-rest",
  "version": "1.0.0",
  "description": "Fake REST Api for .NET project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --watch db.json --host 192.168.61.131 db.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
